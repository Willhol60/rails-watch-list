<%# navbar %>
<div class="navbar-lewagon">
<%= image_tag "film_reel.png", height: 36 %>
<%= link_to 'Create a new list', new_list_path %>
</div>

<div class="container">
  <% @lists.each do |list| %>
    <% if list.photo.nil? %>
      <div class="card-category">
        <%= link_to list.name, list_path(list) %>
      </div>
    <% else %>
      <div class="card-category">
      <%# style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path list.photo.key, height: 100, crop: :fit')"> %>
        <%= link_to list.name, list_path(list) %>
      </div>
    <% end %>
  <% end %>
</div>

<%#
<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path list.photo, height: 100, crop: :fit %>
        <%# <%= link_to list.name, list_path(list) %>
      <%# </div> %>
